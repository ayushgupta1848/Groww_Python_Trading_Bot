__pycache__/
.venv/
env/
.env
*.pyc
.vscode/
.idea/
echo instrument.json >> .gitignore
instrument.json
*.log
ai_config.json
.chatgpt_profile/
# Groww access-token cache (credential material)
.groww_token.json
.groww_token.json.lock
.groww_token.json.tmp

# macOS junk
.DS_Store

# Browser capture with live session cookies — never commit
*.har

# Heavy runtime logs
trading_decision_engine/logs/
logs/chart_level/

# PROD10 bridge runtime files
.prod10_bridge.lock
.prod10_bridge_cmd.json.claimed.*

# Release notes are runtime state — the v1.0.0 seed lives in LIVE_DASHBOARD._DEFAULT_RELEASE_NOTES
release_notes.json
release_notes.json.tmp

# AUTO-FIT signal history — runtime state, rebuilt every session
.autofit_history.json
.autofit_history.json.tmp
